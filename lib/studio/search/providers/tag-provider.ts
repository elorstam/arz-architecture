/* eslint-disable @typescript-eslint/no-explicit-any -- Server-only Supabase projection row. */
import type{StudioSearchProvider}from"../search-provider";import{searchOr}from"../search-utils";import{db,limitResults}from"./provider-helpers";export const tagSearchProvider:StudioSearchProvider={id:"tags",getLabel:()=>"Etiketler",getIcon:()=>"tag",getCategory:()=>"tags",getUrl:()=>"/studio/settings/tags",async search(query,context){const{data,error}=await db(context).from("studio_tags").select("id,name,color,description,is_archived").eq("organization_id",context.organizationId).eq("is_archived",false).or(searchOr(["name","normalized_name","description"],query)).limit(10);if(error)return[];return limitResults((data??[]).map((row:any)=>({id:row.id,provider:this.id,category:this.getCategory(),categoryLabel:this.getLabel(),icon:this.getIcon(),title:row.name,subtitle:row.description||"Organizasyon etiketi",breadcrumb:"Etiketler ve Taksonomi",badge:row.color,url:this.getUrl(row),searchText:[row.name,row.description].join(" ")})),query);}};
