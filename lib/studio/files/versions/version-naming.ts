import {fileExtension} from "../file-validation.ts";
import {normalizeOptionalText,normalizeRequiredText} from "./version-text-normalization.ts";
function safeMarker(value:string){return value.normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-zA-Z0-9_-]/g,"-").replace(/-+/g,"-").replace(/^-|-$/g,"").slice(0,40);}
export function physicalVersionName(logicalName:unknown,versionNumber:number,revisionCode:unknown=null){const name=normalizeRequiredText(logicalName,"logical_file_name","physical_version_name");const extension=fileExtension(name);if(!extension)normalizeRequiredText(null,"logical_file_extension","physical_version_name");const base=name.slice(0,-extension.length-1).replace(/__V\d+(?:__[^.]*)?$/i,"").trim();const optionalRevision=normalizeOptionalText(revisionCode,"revision_code","physical_version_name");const revision=optionalRevision?safeMarker(optionalRevision):"";return`${base}__V${versionNumber}${revision?`__${revision}`:""}.${extension}`;}
export function versionDownloadName(logicalName:string,versionNumber:number){const extension=fileExtension(logicalName);const base=logicalName.slice(0,-extension.length-1);return`${base}_V${versionNumber}.${extension}`;}
