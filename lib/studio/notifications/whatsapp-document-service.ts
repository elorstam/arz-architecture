import"server-only";import{getStudioContext}from"@/lib/studio/auth/get-studio-context";import{createStudioProjectFileDownload}from"@/lib/studio/files/file-repository";import{createStudioServerClient}from"@/lib/studio/supabase/server";import{sendWhatsAppDocument,uploadWhatsAppDocument}from"./whatsapp-media-adapter";
export async function sendStudioDocumentNotification(input:{projectId:string;sourceType:"fee"|"project_stage";sourceId:string;fileId:string;templateName:string;variables:Record<string,string>}){const c=await getStudioContext();if(!c?.user||!c.membership||c.membership.role!=="owner")throw new Error("forbidden");const db=await createStudioServerClient(),org=c.membership.organization_id;const{data:p}=await db.from("studio_projects").select("name,client_name,client_phone").eq("id",input.projectId).eq("organization_id",org).single();if(!p?.client_phone)throw new Error("customer_phone_missing");const{data:leads}=await db.from("studio_leads").select("id,whatsapp_opt_in,whatsapp_phone").eq("organization_id",org).eq("phone",p.client_phone).eq("is_archived",false).limit(2);if(leads?.length!==1)throw new Error("crm_customer_missing");if(!leads[0].whatsapp_opt_in)throw new Error("whatsapp_opt_in_required");const{data:existing}=await db.from("studio_notifications").select("id").eq("organization_id",org).eq("source_type",input.sourceType).eq("source_id",input.sourceId).eq("template_name",input.templateName).in("status",["queued","sent","delivered","read"]).limit(1).maybeSingle();if(existing)return{ok:true as const,idempotent:true};const download=await createStudioProjectFileDownload(input.projectId,input.fileId),response=download.kind==="stream"?download.response:await fetch(download.url,{cache:"no-store"});const mime=response.headers.get("content-type")?.split(";")[0]??"";const{data:n,error}=await db.from("studio_notifications").insert({organization_id:org,project_id:input.projectId,crm_lead_id:leads[0].id,source_type:input.sourceType,source_id:input.sourceId,channel:"whatsapp",status:"queued",template_name:input.templateName,template_version:1,variables_snapshot:input.variables,recipient_snapshot:{phone:"***"},created_by:c.user.id}).select("id").single();if(error)throw error;const media=await uploadWhatsAppDocument(response,download.fileName,mime);if(!media.ok){await db.from("studio_notifications").update({status:"failed",safe_error_code:media.errorCode}).eq("id",n.id);throw new Error(media.errorCode)}const sent=await sendWhatsAppDocument({phone:leads[0].whatsapp_phone||p.client_phone,templateName:input.templateName,parameters:Object.values(input.variables),mediaId:media.mediaId,fileName:download.fileName});await db.from("studio_notification_attachments").insert({organization_id:org,notification_id:n.id,file_id:input.fileId,status:sent.ok?"sent":"failed",provider_media_id:media.mediaId,provider_message_id:sent.ok?sent.messageId:null,safe_error_code:sent.ok?null:sent.errorCode});await db.from("studio_notifications").update(sent.ok?{status:"sent",provider_message_id:sent.messageId,sent_at:new Date().toISOString()}:{status:"failed",safe_error_code:sent.errorCode}).eq("id",n.id);if(!sent.ok)throw new Error(sent.errorCode);return{ok:true as const,idempotent:false}}
