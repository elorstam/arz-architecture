import"server-only";import pdfMake from"pdfmake/build/pdfmake";import pdfFonts from"pdfmake/build/vfs_fonts";import type{FinanceDashboard}from"./finance-types";import{money}from"./finance-validation";pdfMake.addVirtualFileSystem(pdfFonts);
export async function financeReportPdf(data:FinanceDashboard){const body=[["Tür","Kayıt","Proje","Tarih","Durum","Tutar"],...data.entries.map(x=>[x.type,x.title,x.projectName??"—",x.entryDate,x.status,money(x.amount,x.currency)])];const projects=data.profitability.map(x=>[x.projectName,money(x.expected),money(x.collected),money(x.remaining),money(x.expense),money(x.pendingExpense),money(x.profit),x.margin===null?"—":`%${x.margin.toFixed(1)}`]);return pdfMake.createPdf({content:[{text:"ARZ Studio Finans Raporu",style:"header"},{text:`Anlaşılan ${money(data.summary.totalExpected)} · Tahsilat ${money(data.summary.totalCollected)} · Gider ${money(data.summary.totalExpense)} · Brüt Kâr ${money(data.summary.grossProfit)}`,margin:[0,8,0,16]},{table:{headerRows:1,widths:[65,"*","*",65,65,75],body},fontSize:8},{text:"Proje Kârlılığı",style:"header",margin:[0,16,0,8]},{table:{headerRows:1,widths:["*",70,70,70,70,70,70,55],body:[["Proje","Anlaşılan","Tahsilat","Kalan","Gider","Bekleyen","Brüt Kâr","Marj"],...projects]},fontSize:8}],styles:{header:{fontSize:18,bold:true}},defaultStyle:{font:"Roboto"}}).getBuffer()}
export function financeCsv(data:FinanceDashboard,separator=","){const quote=(value:unknown)=>`"${String(value??"").replaceAll('"','""')}"`;return[["Tür","Başlık","Proje","Tarih","Durum","Tutar","KDV","Para Birimi"],...data.entries.map(x=>[x.type,x.title,x.projectName,x.entryDate,x.status,x.amount,x.taxRate,x.currency]),[],["Proje Kârlılığı","Anlaşılan","Tahsilat","Kalan Tahsilat","Toplam Gider","Bekleyen Gider","Brüt Kâr","Kâr Marjı"],...data.profitability.map(x=>[x.projectName,x.expected,x.collected,x.remaining,x.expense,x.pendingExpense,x.profit,x.margin])].map(row=>row.map(quote).join(separator)).join("\r\n")}
