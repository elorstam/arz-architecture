import{z}from"zod";import{STUDIO_TAG_COLORS}from"./tag-colors";import{STUDIO_TAG_ENTITY_TYPES}from"./tag-types";export const tagSchema=z.object({name:z.string().trim().min(1,"Etiket adı zorunludur.").max(60),color:z.enum(STUDIO_TAG_COLORS),description:z.string().trim().max(500).default("")});export const assignmentSchema=z.object({tagId:z.string().uuid(),entityType:z.enum(STUDIO_TAG_ENTITY_TYPES),entityId:z.string().uuid()});
