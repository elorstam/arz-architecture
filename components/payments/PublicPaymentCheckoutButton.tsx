"use client";
import{useState}from"react";
export default function PublicPaymentCheckoutButton({token}:{token:string}){const[pending,setPending]=useState(false),[message,setMessage]=useState("");async function start(){if(pending)return;setPending(true);setMessage("");try{const response=await fetch(`/api/payments/public/${encodeURIComponent(token)}/checkout`,{method:"POST",headers:{"Content-Type":"application/json"},body:"{}"}),body=await response.json().catch(()=>null)as{checkoutUrl?:string;error?:string}|null;if(response.ok&&body?.checkoutUrl){location.assign(body.checkoutUrl);return;}setMessage(body?.error??"Ödeme başlatılamadı. Lütfen tekrar deneyin.");}catch{setMessage("Ödeme başlatılamadı. Lütfen tekrar deneyin.");}finally{setPending(false)}}return <div className="public-payment-action"><button type="button" disabled={pending} onClick={start}>{pending?"Hazırlanıyor…":"iyzico ile Öde"}</button>{message?<p role="status">{message}</p>:null}</div>}
