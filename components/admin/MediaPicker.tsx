'use client';import {useEffect,useState} from 'react';import type {MediaItem} from '@/types/cms';
export default function MediaPicker({onSelect}:{onSelect:(url:string)=>void}){const[items,setItems]=useState<MediaItem[]>([]);useEffect(()=>{fetch('/api/admin/media').then(r=>r.json()).then(setItems).catch(()=>setItems([]))},[]);return <select aria-label="Medya kütüphanesinden görsel seç" defaultValue="" onChange={e=>{if(e.target.value)onSelect(e.target.value);e.currentTarget.value=''}} className="admin-select mt-2 w-full text-sm"><option value="">Medya kütüphanesinden seç…</option>{items.map(x=><option key={x.id} value={x.url}>{x.filename}</option>)}</select>}
