"use client";import{useState}from"react";import StudioAiWritingDialog from"@/components/studio/ai/StudioAiWritingDialog";import type{FinanceDashboard}from"@/lib/studio/finance/finance-types";
export default function StudioFinanceAiWriter({data}:{data:FinanceDashboard}){const[text,setText]=useState("");return <section className="rounded-xl border bg-white p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-lg font-semibold">AI Finans Özeti</h2><p className="mt-1 text-sm text-[#68706f]">Gerçek finansal toplamları değiştirmeden kısa bir yönetim özeti oluşturun.</p></div><StudioAiWritingDialog operation="finance_summary" title="AI Finans Özeti" triggerLabel="AI Özet Oluştur" currentText={text} context={{income:String(data.summary.income),expense:String(data.summary.expense),collected:String(data.summary.collected),outstanding:String(data.summary.outstanding),profit:String(data.summary.profit)}} onUse={setText}/></div>{text?<textarea aria-label="AI finans özeti" value={text} onChange={event=>setText(event.target.value)} className="studio-field mt-4 min-h-32 w-full"/>:null}</section>}
