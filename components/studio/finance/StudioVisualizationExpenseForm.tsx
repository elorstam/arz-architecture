"use client";
import {useState} from "react";
import {createProjectExpenseAction} from "@/app/studio/(protected)/projects/[projectId]/finance/actions";
import StudioAiWritingDialog from "@/components/studio/ai/StudioAiWritingDialog";
import {studioButtonClass} from "@/components/studio/StudioButton";
const categories=["render_farm","freelance_modelleme","freelance_render","asset_model","texture_material","stock_visual","ai_credits","software_license","plugin","animation","video_editing","sound_license","hardware_rental","outsourced_service","other"];
export default function StudioVisualizationExpenseForm({projectId,projectName}:{projectId:string;projectName:string}){const[description,setDescription]=useState("");const[result,setResult]=useState("");return <section className="rounded-2xl border bg-white p-5"><div className="flex flex-wrap items-center justify-between gap-3"><h2 className="text-xl font-semibold">Görselleştirme Gideri Ekle</h2><span className="text-sm text-[#68706f]">Ortak proje finans kaydı</span></div><form action={async form=>setResult((await createProjectExpenseAction(projectId,form)).message)} className="mt-4 grid min-w-0 gap-3 sm:grid-cols-2 lg:grid-cols-4"><label className="text-sm font-semibold">Kategori<select name="category" className="studio-field mt-1 w-full">{categories.map(x=><option key={x} value={x}>{x.replaceAll("_"," ")}</option>)}</select></label><label className="text-sm font-semibold">Tutar<input name="amount" required type="number" min="0.01" step="0.01" className="studio-field mt-1 w-full"/></label><label className="text-sm font-semibold">Ödenen tutar<input name="paidAmount" type="number" min="0" step="0.01" defaultValue="0" className="studio-field mt-1 w-full"/></label><label className="text-sm font-semibold">Ödeme durumu<select name="status" defaultValue="waiting" className="studio-field mt-1 w-full"><option value="waiting">Bekliyor</option><option value="partial">Kısmi Ödendi</option><option value="paid">Ödendi</option><option value="cancelled">İptal</option></select></label><label className="text-sm font-semibold">Tarih<input name="entryDate" required type="date" className="studio-field mt-1 w-full"/></label><label className="text-sm font-semibold sm:col-span-2">Açıklama<span className="ml-2 inline-flex"><StudioAiWritingDialog operation="visualization_expense_description" title="Gider açıklaması" triggerLabel="AI ile Açıklama Oluştur" context={{projectName,category:"görselleştirme gideri",amount:""}} onUse={setDescription}/></span><textarea name="description" required value={description} onChange={e=>setDescription(e.target.value)} className="studio-field mt-1 min-h-20 w-full"/></label><button className={studioButtonClass("primary","md")}>Kaydet</button></form>{result?<p role="status" className="mt-3 text-sm">{result}</p>:null}</section>}
