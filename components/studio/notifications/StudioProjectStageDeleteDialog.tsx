"use client";
import {useState,useTransition} from "react";
import {deleteStageAction} from "@/app/studio/(protected)/projects/[projectId]/stages/actions";
import {studioButtonClass} from "@/components/studio/StudioButton";
export default function StudioProjectStageDeleteDialog({projectId,stageTitle,stageId,attachmentCount=0,notificationCount=0,eventCount=0,onClose}:{projectId:string;stageTitle:string;stageId:string;attachmentCount?:number;notificationCount?:number;eventCount?:number;onClose:()=>void}){const[confirmation,setConfirmation]=useState("");const[busy,start]=useTransition();const matches=confirmation.trim()===stageTitle.trim();return <div role="dialog" aria-modal="true" aria-labelledby={`delete-stage-${stageId}`} className="fixed inset-0 z-[60] grid place-items-center bg-black/50 p-4"><div className="w-full max-w-lg rounded-2xl bg-white p-6 shadow-xl"><h2 id={`delete-stage-${stageId}`} className="text-xl font-semibold">Özel Aşamayı Kalıcı Sil</h2><p className="mt-2 text-sm text-slate-700">Bu işlem geri alınamaz.</p><dl className="mt-4 grid grid-cols-2 gap-2 text-sm"><div><dt className="text-slate-500">Aşama</dt><dd className="break-words font-semibold">{stageTitle}</dd></div><div><dt className="text-slate-500">Ek dosyası</dt><dd className="font-semibold">{attachmentCount}</dd></div><div><dt className="text-slate-500">Bildirim</dt><dd className="font-semibold">{notificationCount}</dd></div><div><dt className="text-slate-500">Timeline olayı</dt><dd className="font-semibold">{eventCount}</dd></div></dl><label className="mt-5 block text-sm font-semibold">Devam etmek için aşama başlığını yazın<input autoFocus value={confirmation} onChange={event=>setConfirmation(event.target.value)} className="mt-2 h-11 w-full rounded-lg border px-3" /></label><div className="mt-5 flex flex-wrap justify-end gap-2"><button type="button" onClick={onClose} className={studioButtonClass("ghost","sm")}>Vazgeç</button><button type="button" disabled={!matches||busy} onClick={()=>start(async()=>{await deleteStageAction(projectId,stageId,confirmation);onClose();})} className={studioButtonClass("danger","sm")}>Kalıcı Olarak Sil</button></div></div></div>}
