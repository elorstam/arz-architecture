"use client";import{useActionState,useState,useTransition}from"react";import{archiveStageAction,attachStageFileAction,createStageAction,duplicateStageAction,removeStageFileAction,reorderStageAction,sendStageNotificationAction,toggleStageFileVisibilityAction,updateStageAction}from"@/app/studio/(protected)/projects/[projectId]/stages/actions";import{studioButtonClass}from"@/components/studio/StudioButton";import{PROJECT_STAGE_STATUSES,PROJECT_STAGE_STATUS_LABELS,type NotificationActionState,type StudioNotification,type StudioProjectStage}from"@/lib/studio/notifications/notification-types";const initial:NotificationActionState={success:false,message:""};type Attachment={id:string;stageId:string;fileId:string;name:string;extension:string;customerVisible:boolean};type FileOption={id:string;name:string;extension:string};
export default function StudioProjectStages({projectId,stages,notifications,attachments,files,canManage,configured}:{projectId:string;stages:StudioProjectStage[];notifications:StudioNotification[];attachments:Attachment[];files:FileOption[];canManage:boolean;configured:boolean}){const active=stages.filter(s=>s.isActive&&!s.isArchived&&s.status!=="cancelled"),done=active.filter(s=>s.status==="completed").length,progress=active.length?Math.round(done/active.length*100):0;return <><section className="mt-6 rounded-xl border bg-white p-5"><div className="flex justify-between"><h2 className="text-xl font-semibold">İlerleme</h2><strong>%{progress}</strong></div><div role="progressbar" aria-valuenow={progress} aria-valuemin={0} aria-valuemax={100} className="mt-3 h-3 overflow-hidden rounded-full bg-stone-200"><div className="h-full bg-[#a68446]" style={{width:`${progress}%`}}/></div>{!configured?<p role="status" className="mt-3 text-sm">WhatsApp yapılandırılmadı.</p>:null}</section>{canManage?<New projectId={projectId}/>:null}<ol className="mt-5 space-y-4">{stages.map(s=><Stage key={s.id} projectId={projectId} stage={s} attachments={attachments.filter(a=>a.stageId===s.id)} files={files} canManage={canManage} configured={configured}/>)}</ol><section className="mt-6 rounded-xl border bg-white p-5"><h2 className="text-xl font-semibold">Gönderim geçmişi</h2><ul className="mt-3">{notifications.map(n=><li key={n.id} className="py-2 text-sm">{n.templateName} · {n.status}</li>)}</ul></section></>}
function New({projectId}:{projectId:string}){const[state,action,pending]=useActionState(createStageAction.bind(null,projectId),initial);return <form action={action} className="mt-5 flex min-w-0 flex-col gap-3 rounded-xl border p-4 sm:flex-row sm:items-end"><label className="min-w-0 flex-1 text-sm font-semibold">Yeni aşama<input required name="title" className="mt-1 h-11 w-full min-w-0 rounded-lg border px-3"/></label><button disabled={pending} className={studioButtonClass("primary","md","shrink-0")}>Yeni Aşama Ekle</button>{state.message?<p role={state.success?"status":"alert"}>{state.message}</p>:null}</form>}
function Stage({projectId,stage,attachments,files,canManage,configured}:{projectId:string;stage:StudioProjectStage;attachments:Attachment[];files:FileOption[];canManage:boolean;configured:boolean}){const[state,action,pending]=useActionState(updateStageAction.bind(null,projectId,stage.id),initial);const[busy,start]=useTransition();const[preview,setPreview]=useState(false);const disabled=!configured||!stage.isActive||stage.isArchived||stage.status!=="completed";return <li className={`min-w-0 rounded-xl border bg-white p-4 ${stage.isArchived?"opacity-60":""}`}><div className="flex flex-wrap items-start justify-between gap-3"><h3 className="min-w-0 break-words text-lg font-semibold">{stage.sortOrder}. {stage.title}</h3>{canManage?<div className="flex flex-wrap gap-2"><button onClick={()=>start(async()=>{await reorderStageAction(projectId,stage.id,-1)})} className={studioButtonClass("ghost","sm")}>↑ Yukarı</button><button onClick={()=>start(async()=>{await reorderStageAction(projectId,stage.id,1)})} className={studioButtonClass("ghost","sm")}>↓ Aşağı</button><button onClick={()=>start(async()=>{await duplicateStageAction(projectId,stage.id)})} className={studioButtonClass("outline","sm")}>Çoğalt</button><button onClick={()=>start(async()=>{await archiveStageAction(projectId,stage.id,!stage.isArchived)})} className={studioButtonClass("outline","sm")}>{stage.isArchived?"Geri Al":"Arşivle"}</button></div>:null}</div>{canManage?<form action={action} className="mt-4 grid gap-3 md:grid-cols-2"><label>Başlık<input name="title" defaultValue={stage.title} className="h-11 w-full rounded-lg border px-3"/></label><label>Durum<select name="status" defaultValue={stage.status} className="h-11 w-full rounded-lg border px-3">{PROJECT_STAGE_STATUSES.map(v=><option key={v} value={v}>{PROJECT_STAGE_STATUS_LABELS[v]}</option>)}</select></label><label>Açıklama<input name="description" defaultValue={stage.description} className="h-11 w-full rounded-lg border px-3"/></label><label>Başlangıç<input type="date" name="startedAt" defaultValue={stage.startedAt?.slice(0,10)} className="h-11 w-full rounded-lg border px-3"/></label><label>Tamamlanma<input type="date" name="completedAt" defaultValue={stage.completedAt?.slice(0,10)} className="h-11 w-full rounded-lg border px-3"/></label><label>Not<input name="note" defaultValue={stage.note} className="h-11 w-full rounded-lg border px-3"/></label><label><input type="checkbox" name="isActive" defaultChecked={stage.isActive}/> Aktif</label><label><input type="checkbox" name="isClientVisible" defaultChecked={stage.isClientVisible}/> Müşteriye görünür</label><button disabled={pending} className={studioButtonClass("primary","sm")}>Aşamayı Kaydet</button>{state.message?<p role={state.success?"status":"alert"}>{state.message}</p>:null}</form>:<p className="mt-3 text-sm">{stage.description}</p>}<div className="mt-4 border-t pt-4"><h4 className="font-semibold">Ekler: {attachments.length}</h4><ul>{attachments.map(a=><li key={a.id} className="flex min-w-0 flex-wrap items-center gap-2 py-2 text-sm"><span className="min-w-0 flex-1 break-all">{a.name}</span><a href={`/studio/projects/${projectId}/files/${a.fileId}`} className={studioButtonClass("ghost","sm")}>Dosyayı Gör</a><a href={`/studio/projects/${projectId}/files/${a.fileId}/download`} className={studioButtonClass("ghost","sm")}>İndir</a>{canManage?<><button onClick={()=>start(async()=>{await toggleStageFileVisibilityAction(projectId,a.id,!a.customerVisible)})} className={studioButtonClass("outline","sm")}>{a.customerVisible?"Gönderimden Çıkar":"Müşteriye Gönder"}</button><button onClick={()=>start(async()=>{await removeStageFileAction(projectId,a.id)})} className={studioButtonClass("outline","sm")}>Bağlantıyı Kaldır</button></>:null}</li>)}</ul>{canManage?<Attach projectId={projectId} stageId={stage.id} files={files}/>:null}</div><button disabled={disabled||busy} title={disabled?(!configured?"WhatsApp henüz yapılandırılmadı.":"Yalnız aktif, arşivlenmemiş ve tamamlanan aşamalar gönderilebilir."):undefined} onClick={()=>setPreview(true)} className={studioButtonClass("secondary","sm","mt-4")}>Müşteriye Bildir</button>{preview?<div role="dialog" aria-modal="true" className="fixed inset-0 z-50 grid place-items-center bg-black/50 p-4"><div className="max-w-lg rounded-xl bg-white p-6"><h4 className="text-xl font-semibold">Mesaj önizleme</h4><p className="mt-3 text-sm">Güncel aşama adı, açıklaması ve müşteriye görünür PDF ekleri gönderilecek. DWG ekleri desteklenmiyorsa işlem kısmi hata olarak durdurulur.</p><div className="mt-4 flex justify-end gap-2"><button onClick={()=>setPreview(false)} className={studioButtonClass("ghost","sm")}>İptal</button><button onClick={()=>start(async()=>{await sendStageNotificationAction(projectId,stage.id);setPreview(false)})} className={studioButtonClass("primary","sm")}>Onayla ve Gönder</button></div></div></div>:null}</li>}
function Attach({projectId,stageId,files}:{projectId:string;stageId:string;files:FileOption[]}){const[state,action,pending]=useActionState(attachStageFileAction.bind(null,projectId,stageId),initial);return <form action={action} className="mt-3 flex flex-wrap gap-2"><select required name="fileId" className="h-10 min-w-0 flex-1 rounded-lg border px-2"><option value="">PDF/DWG seçin</option>{files.map(f=><option key={f.id} value={f.id}>{f.name}</option>)}</select><select name="role" className="h-10 rounded-lg border"><option value="approval_document">Onay belgesi</option><option value="drawing">Çizim</option><option value="report">Rapor</option><option value="other">Diğer</option></select><label><input type="checkbox" name="customerVisible"/> Müşteriye gönder</label><button disabled={pending} className={studioButtonClass("outline","sm")}>Dosya Bağla</button>{state.message?<p role={state.success?"status":"alert"}>{state.message}</p>:null}</form>}
