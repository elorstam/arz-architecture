"use client";
import Link from "next/link";
import {useActionState} from "react";
import {updateFileMetadataAction} from "@/app/studio/(protected)/projects/[projectId]/files/actions";
import {studioButtonClass,StudioPendingLabel} from "@/components/studio/StudioButton";
import {STUDIO_CATEGORY_LABELS,STUDIO_FILE_CATEGORIES} from "@/lib/studio/files/file-constants";
import type {StudioProjectFile,StudioProjectFolder} from "@/lib/studio/files/file-types";
import StudioFileArchiveControl from "./StudioFileArchiveControl";
import StudioFileTypeIcon from "./StudioFileTypeIcon";
function Item({label,children}:{label:string;children:React.ReactNode}){return <div><dt className="text-[8px] font-semibold uppercase tracking-[.1em] text-[#8c908c]">{label}</dt><dd className="mt-1.5 break-words text-xs text-[#374147]">{children}</dd></div>;}
export default function StudioFileDetail({file,folders}:{file:StudioProjectFile;folders:StudioProjectFolder[]}){const[state,action,pending]=useActionState(updateFileMetadataAction.bind(null,file.projectId,file.id),{success:false});return <section className="mx-auto min-w-0 max-w-[1100px] px-4 py-6 sm:px-6 sm:py-8 lg:px-8"><header className="rounded-xl border border-[#dedad1] bg-white p-5 sm:p-7"><Link href={`/studio/projects/${file.projectId}/files${file.folderId?`?folder=${file.folderId}`:""}`} className="text-[9px] font-semibold uppercase tracking-[.14em] text-[#8e784d]">← Dosyalara Dön</Link><div className="mt-5 flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div className="flex min-w-0 gap-4"><StudioFileTypeIcon extension={file.extension}/><div className="min-w-0"><h1 className="break-words text-2xl font-semibold tracking-[-.03em]">{file.displayName}</h1><p className="mt-2 text-[10px] text-[#6e7573]">{file.extension.toUpperCase()} · {file.fileSizeLabel} · {file.status}{file.isArchived?" · Arşiv":""}</p></div></div><div className="flex flex-wrap gap-2"><a href={`/studio/projects/${file.projectId}/files/${file.id}/download`} className={studioButtonClass("primary","sm")}>Güvenli İndir</a>{file.canManage?<StudioFileArchiveControl projectId={file.projectId} fileId={file.id} archived={file.isArchived}/>:null}</div></div></header><div className="mt-5 grid gap-5 lg:grid-cols-2"><section className="rounded-xl border border-[#dedad1] bg-white p-5 sm:p-6"><h2 className="text-sm font-semibold">Dosya Bilgileri</h2><dl className="mt-5 grid gap-5 sm:grid-cols-2"><Item label="Orijinal ad">{file.originalFileName}</Item><Item label="MIME">{file.mimeType}</Item><Item label="Kategori">{STUDIO_CATEGORY_LABELS[file.category]}</Item><Item label="Boyut">{file.fileSizeLabel}</Item><Item label="Yükleyen">{file.uploadedBy.name}</Item><Item label="Yüklenme">{file.createdAtLabel}</Item><Item label="Durum">{file.status}</Item><Item label="Güncellenme">{new Intl.DateTimeFormat("tr-TR",{dateStyle:"medium",timeStyle:"short"}).format(new Date(file.updatedAt))}</Item></dl><div className="mt-5 border-t pt-5"><p className="text-[8px] font-semibold uppercase tracking-[.1em] text-[#8c908c]">Açıklama</p><p className="mt-2 whitespace-pre-wrap text-xs leading-6 text-[#596260]">{file.description||"Açıklama eklenmemiş."}</p></div></section><section className="rounded-xl border border-[#dedad1] bg-white p-5 sm:p-6"><h2 className="text-sm font-semibold">Metadata Düzenle</h2>{file.canManage?<form action={action} className="mt-5 space-y-4"><label className="block text-[9px] font-semibold uppercase tracking-[.1em] text-[#707674]">Görünen Ad<input name="displayName" required maxLength={240} defaultValue={file.displayName} className="mt-2 h-11 w-full rounded-lg border bg-[#fbfaf7] px-3 text-sm"/></label><label className="block text-[9px] font-semibold uppercase tracking-[.1em] text-[#707674]">Kategori<select name="category" defaultValue={file.category} className="mt-2 h-11 w-full rounded-lg border bg-white px-3 text-sm">{STUDIO_FILE_CATEGORIES.map(v=><option key={v} value={v}>{STUDIO_CATEGORY_LABELS[v]}</option>)}</select></label><label className="block text-[9px] font-semibold uppercase tracking-[.1em] text-[#707674]">Klasör<select name="folderId" defaultValue={file.folderId} className="mt-2 h-11 w-full rounded-lg border bg-white px-3 text-sm"><option value="">Kök</option>{folders.map(folder=><option key={folder.id} value={folder.id}>{folder.name}</option>)}</select></label><label className="block text-[9px] font-semibold uppercase tracking-[.1em] text-[#707674]">Açıklama<textarea name="description" rows={5} maxLength={5000} defaultValue={file.description} className="mt-2 w-full rounded-lg border bg-[#fbfaf7] p-3 text-sm"/></label><p role="alert" className="text-[10px] text-[#8b5141]">{state.message}</p><button disabled={pending} aria-busy={pending} className={studioButtonClass("primary")}><StudioPendingLabel pending={pending} pendingLabel="Kaydediliyor…">Değişiklikleri Kaydet</StudioPendingLabel></button></form>:<p className="mt-4 text-sm text-[#69716f]">Metadata düzenleme yalnızca Studio sahibine açıktır.</p>}</section></div></section>;}
