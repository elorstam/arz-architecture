import StudioFileTypeIcon from "../StudioFileTypeIcon";
import {studioButtonClass} from "@/components/studio/StudioButton";
export default function StudioUnsupportedPreview({extension,downloadUrl}:{extension:string;downloadUrl:string}){return <div className="flex min-h-[360px] flex-col items-center justify-center rounded-xl border border-dashed border-[#cec7bb] bg-[#f8f6f1] p-8 text-center"><StudioFileTypeIcon extension={extension}/><h3 className="mt-5 text-xl font-semibold">Dosya önizlemesi desteklenmiyor.</h3><p className="mt-2 max-w-md text-[15px] leading-6 text-[#69716f]">{extension.toUpperCase()} dosyasını güvenli indirme bağlantısıyla bilgisayarınızda açabilirsiniz.</p><a href={downloadUrl} className={`${studioButtonClass("primary")} mt-5`}>Dosyayı İndir</a></div>;}
