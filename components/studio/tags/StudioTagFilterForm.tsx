import{listStudioTags}from"@/lib/studio/tags/tag-repository";export default async function StudioTagFilterForm({selected,mode}:{selected:string[];mode:"any"|"all"}){const{tags}=await listStudioTags();if(!tags.length)return null;return <form className="mx-auto mt-4 flex max-w-[1540px] flex-wrap items-end gap-3 px-4 sm:px-6 lg:px-8"><label className="text-sm font-semibold">Etiketler<select name="tags" multiple defaultValue={selected} className="mt-2 min-h-20 min-w-48 rounded-lg border bg-white px-3 text-sm">{tags.map(tag=><option key={tag.id} value={tag.id}>{tag.name}</option>)}</select></label><label className="text-sm font-semibold">Eşleşme<select name="tagMode" defaultValue={mode} className="mt-2 block h-11 rounded-lg border bg-white px-3 text-sm"><option value="any">Herhangi biri</option><option value="all">Tümü</option></select></label><button className="studio-button studio-button--secondary studio-button--md">Etiketleri Filtrele</button></form>;}
