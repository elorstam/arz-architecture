import{STUDIO_TAG_COLOR_CLASSES}from"@/lib/studio/tags/tag-colors";import type{StudioTag}from"@/lib/studio/tags/tag-types";export default function StudioTagBadge({tag,onRemove}:{tag:StudioTag;onRemove?:()=>void}){return <span className={`inline-flex max-w-full items-center gap-1.5 rounded-full border px-2.5 py-1 text-[13px] font-semibold ${STUDIO_TAG_COLOR_CLASSES[tag.color]}`}><span className="break-words">{tag.name}</span>{tag.isArchived?<span className="text-[11px]">Arşiv</span>:null}{onRemove?<button type="button" onClick={onRemove} aria-label={`${tag.name} etiketini kaldır`} className="rounded-full px-1 focus:outline-none focus:ring-2">×</button>:null}</span>;}
