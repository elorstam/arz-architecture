"use client";import type{StudioTag}from"@/lib/studio/tags/tag-types";export default function StudioTagFilter({tags,selected,mode,onChange}:{tags:StudioTag[];selected:string[];mode:"any"|"all";onChange:(ids:string[],mode:"any"|"all")=>void}){return <div className="flex flex-wrap gap-3"><select multiple value={selected} onChange={event=>onChange([...event.target.selectedOptions].map(option=>option.value),mode)} className="min-h-11 rounded-lg border px-3 text-sm" aria-label="Etiket filtresi">{tags.filter(tag=>!tag.isArchived).map(tag=><option key={tag.id} value={tag.id}>{tag.name}</option>)}</select><select value={mode} onChange={event=>onChange(selected,event.target.value as"any"|"all")} className="h-11 rounded-lg border px-3 text-sm" aria-label="Etiket eşleşme modu"><option value="any">Herhangi biri</option><option value="all">Tümü</option></select></div>;}
