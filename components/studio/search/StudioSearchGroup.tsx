"use client";
import type{StudioSearchResult}from"@/lib/studio/search/search-types";import StudioSearchItem from"./StudioSearchItem";
export default function StudioSearchGroup({label,results,query,offset,activeIndex,onActivate,onSelect}:{label:string;results:StudioSearchResult[];query:string;offset:number;activeIndex:number;onActivate:(index:number)=>void;onSelect:(result:StudioSearchResult)=>void}){return <section aria-labelledby={`search-group-${results[0]?.category}`}><h3 id={`search-group-${results[0]?.category}`} className="px-4 pb-1 pt-4 text-xs font-bold uppercase tracking-[.12em] text-[#8b7449]">{label}</h3><div className="space-y-1 px-2">{results.map((result,index)=><StudioSearchItem key={`${result.provider}:${result.id}`} result={result} query={query} active={activeIndex===offset+index} onHover={()=>onActivate(offset+index)} onSelect={()=>onSelect(result)}/>)}</div></section>;}
