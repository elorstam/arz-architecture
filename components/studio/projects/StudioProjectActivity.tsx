import type {ProjectActivity} from "@/components/studio/projects/StudioProjectData";
import {StudioActivityIcon,StudioBadge,StudioCard,StudioEmptyState,StudioSectionHeader} from "@/components/studio/ui";

export default function StudioProjectActivity({items}:{items:ProjectActivity[]}){
 return <StudioCard as="section" className="h-full overflow-hidden p-0"><div className="p-4"><StudioSectionHeader title="Son Aktivite" description="Son değişiklikler ve kararlar" icon="activity" count={items.length}/></div>{items.length?<div className="max-h-[300px] overflow-y-auto border-t border-[#e7ecf3] px-4">{items.map(item=><article key={`${item.title}-${item.relativeTime}`} className="grid min-h-[66px] grid-cols-[36px_32px_minmax(0,1fr)_auto] items-center gap-2 border-b border-[#e7ecf3] py-2 last:border-0"><StudioActivityIcon variant="project" size="sm"/><span className="grid h-8 w-8 place-items-center rounded-full bg-[#eef3f8] text-[9px] font-semibold text-[#526b7a]">{item.actorInitials}</span><div className="min-w-0"><h3 className="truncate text-[12px] font-semibold text-[#1e293b]">{item.title}</h3><p className="truncate text-[11px] text-[#64748b]">{item.detail}</p><div className="mt-1"><StudioBadge variant="neutral">{item.type}</StudioBadge></div></div><time className="self-start pt-1 text-[10px] text-[#94a3b8]">{item.relativeTime}</time></article>)}</div>:<StudioEmptyState icon="activity" title="Aktivite bulunmuyor" description="Bu projede henüz kayıtlı aktivite yok."/>}</StudioCard>;
}
