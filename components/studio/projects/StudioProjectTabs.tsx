import Link from "next/link";
const futureTabs=["Renderlar","Revizyonlar","Görevler","Takvim","Müşteri","Finans"] as const;
type ActiveTab="overview"|"files"|"official-processes";
export default function StudioProjectTabs({projectId,active="overview"}:{projectId:string;active?:ActiveTab}){const links=[{id:"overview"as const,label:"Genel Bakış",href:`/studio/projects/${projectId}`},{id:"files"as const,label:"Dosyalar",href:`/studio/projects/${projectId}/files`},{id:"official-processes"as const,label:"Harç ve Evraklar",href:`/studio/projects/${projectId}/official-processes`}];return <nav aria-label="Proje bölümleri" className="mt-5 overflow-hidden rounded-xl border border-[#dedad1] bg-white px-2 shadow-sm"><div className="flex min-w-0 flex-wrap gap-1 py-2">{links.map(link=><Link key={link.id} aria-current={active===link.id?"page":undefined} href={link.href} className={`rounded-lg px-3.5 py-2 text-sm font-semibold ${active===link.id?"bg-[#1c2731] text-white":"text-[#665534] hover:bg-[#f3eee5]"}`}>{link.label}</Link>)}{futureTabs.map(tab=><span key={tab} title="Bu bölüm yakında kullanıma açılacak" className="flex cursor-not-allowed items-center gap-1.5 rounded-lg px-3.5 py-2 text-sm text-[#9b9b96]">{tab}<span className="text-[9px] uppercase tracking-wide text-[#b3a07c]">Yakında</span></span>)}</div></nav>}
