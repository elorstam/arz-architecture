import Link from "next/link";
import StudioCrmFilters from "@/components/studio/crm/StudioCrmFilters";
import StudioLeadCard from "@/components/studio/crm/StudioLeadCard";
import StudioLeadEmptyState from "@/components/studio/crm/StudioLeadEmptyState";
import type {LeadQueryFilters,StudioLead,StudioLeadMember} from "@/lib/studio/crm/lead-types";
import {studioButtonClass} from "@/components/studio/StudioButton";
import {StudioPageHeader} from "@/components/studio/ui";
export default function StudioCrmPage({leads,totalCount,canManage,filters,members,favoriteKeys}:{leads:StudioLead[];totalCount:number;canManage:boolean;filters:LeadQueryFilters;members:StudioLeadMember[];favoriteKeys:Set<string>}){const filtered=Boolean(filters.query||filters.stage||filters.status||filters.serviceType||filters.assignedUserId||filters.archive==="archived");return <section className="mx-auto min-w-0 max-w-[1540px] px-4 py-6 sm:px-6 sm:py-8 lg:px-8 lg:py-9"><StudioPageHeader eyebrow={`Müşteri İlişkileri · ${totalCount} toplam lead`} title="CRM Lead Yönetimi" description="İlk temastan teklif kararına kadar potansiyel müşterilerinizi tek, sakin bir çalışma alanında izleyin." actions={canManage?<Link href="/studio/crm/new" className={studioButtonClass("primary")}>Yeni Lead</Link>:null}/><StudioCrmFilters filters={filters} members={members}/>{leads.length?<div className="mt-5 grid min-w-0 gap-4 md:grid-cols-2 xl:grid-cols-3">{leads.map(lead=><StudioLeadCard key={lead.id} lead={lead} isFavorite={favoriteKeys.has(`crm_lead:${lead.id}`)}/>)}</div>:<StudioLeadEmptyState filtered={filtered||totalCount>0} canManage={canManage}/>}</section>;}
