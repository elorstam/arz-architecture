import assert from "node:assert/strict";
import {readFileSync} from "node:fs";
import test from "node:test";
const read=path=>readFileSync(path,"utf8");
const layout=read("app/client/layout.tsx"),shell=read("components/client-portal/ClientPortalShell.tsx"),header=read("components/client-portal/ClientPortalHeader.tsx"),navigation=read("components/client-portal/ClientPortalNavigation.tsx"),selector=read("components/client-portal/ClientProjectSelector.tsx"),css=read("app/client/client-portal.css");
test("client layout keeps canonical server access guard and shared Studio shell language",()=>{assert.match(layout,/getClientPortalContext/);assert.match(layout,/redirect\("\/studio\/login"\)/);assert.match(layout,/notFound\(\)/);assert.match(layout,/ClientPortalShell/);assert.match(shell,/studio-root client-portal/);assert.doesNotMatch(shell+header,/StudioSidebar|Yeni Proje|admin/i)});
test("client header has brand project context notification profile and secure logout",()=>{assert.match(header,/arz-logo-final\.png/);assert.match(header,/Client Portal/);assert.match(header,/ClientProjectSelector/);assert.match(header,/StudioIconButton/);assert.match(header,/StudioTooltip/);assert.match(header,/\/api\/studio\/auth\/logout/)});
test("project selector only exposes safe projects and preserves selected project in URL",()=>{assert.match(selector,/projects\.length<=1/);assert.match(selector,/projects\.some\(project=>project\.id===requested\)/);assert.match(selector,/next\.set\("project"/);assert.doesNotMatch(selector,/organization_id|storage_path|service.role/i)});
test("client navigation exposes live overview stages renders and files with honest future modules",()=>{for(const label of["Genel Bakış","Proje Aşamaları","Renderlar","Dosyalar","Evraklar","Finans / Ödemeler","Bildirimler","Profil"])assert.match(navigation,new RegExp(label.replace("/","\\/")));for(const href of["/client","/client/stages","/client/renders","/client/files"])assert.match(navigation,new RegExp(`href:"${href.replaceAll("/","\\/")}"`));assert.equal((navigation.match(/badge:"Yakında"/g)??[]).length,4);assert.match(navigation,/workspace-navigation/)});
test("shell is neutral responsive scroll safe and motion aware",()=>{for(const color of["#f7f9fc","#fff","#f3f6fa","#e3e9ef"])assert.match(css,new RegExp(color));assert.doesNotMatch(css,/cream|beige|ivory|warm sand|#fbfaf7|#faf8f3/i);assert.match(shell,/overflow-x-hidden/);assert.match(css,/prefers-reduced-motion/);assert.match(css,/@media\(max-width:767px\)/)});
