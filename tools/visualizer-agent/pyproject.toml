[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "arz-visualizer-agent"
version = "0.1.0"
description = "ARZ Visualizer local agent foundation"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
arz-visualizer-agent = "arz_visualizer_agent.cli:main"
