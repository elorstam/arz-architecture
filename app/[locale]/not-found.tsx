"use client";import Link from "next/link";import {useLocale,useTranslations} from "next-intl";
export default function NotFound(){const locale=useLocale();const t=useTranslations("CMS");return <main className="flex min-h-screen items-center justify-center bg-[#090909] px-6 text-white"><div className="text-center"><p className="text-[10px] uppercase tracking-[.4em] text-white/35">404</p><h1 className="mt-6 text-[clamp(3rem,8vw,8rem)] font-light tracking-[-.06em]">{t('notFound.title')}</h1><p className="mx-auto mt-6 max-w-xl text-white/50">{t('notFound.body')}</p><Link href={`/${locale}`} className="mt-10 inline-block border-b border-white/40 pb-2 text-[10px] uppercase tracking-[.3em]">{t('notFound.back')}</Link></div></main>}
