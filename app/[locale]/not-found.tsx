"use client";
import Link from "next/link";
import {useLocale} from "next-intl";
export default function NotFound(){const locale=useLocale();const en=locale==="en";return <main className="flex min-h-screen items-center justify-center bg-[#090909] px-6 text-white"><div className="text-center"><p className="text-[10px] uppercase tracking-[0.4em] text-white/35">404</p><h1 className="mt-6 text-[clamp(3rem,8vw,8rem)] font-light tracking-[-0.06em]">{en?"Page not found":"Sayfa bulunamadı"}</h1><p className="mx-auto mt-6 max-w-xl text-white/50">{en?"The page you are looking for may have moved or no longer exists.":"Aradığınız sayfa taşınmış veya artık mevcut olmayabilir."}</p><Link href={`/${locale}`} className="mt-10 inline-block border-b border-white/40 pb-2 text-[10px] uppercase tracking-[0.3em]">{en?"Back to Home":"Ana Sayfaya Dön"}</Link></div></main>}
