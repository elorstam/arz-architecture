import type {Metadata} from 'next';import Hero from '@/components/Hero';import {getSiteMessages} from '@/lib/site-translation-store';import {locales} from '@/i18n/locales';
export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const{locale}=await params;const copy=await getSiteMessages(locale);return{title:copy['about.title'],description:copy['about.intro'],alternates:{canonical:`/${locale}`,languages:Object.fromEntries(locales.map(l=>[l,`/${l}`]))},openGraph:{locale,url:`/${locale}`,title:copy['about.title'],description:copy['about.intro']}}}
export default function Page(){return <Hero/>}
