import type {Metadata} from 'next';import AboutPage from '@/components/AboutPage';import {getSiteMessages} from '@/lib/site-translation-store';import {locales} from '@/i18n/locales';
export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const{locale}=await params;const copy=await getSiteMessages(locale);return{title:copy['about.label'],description:copy['about.intro'],openGraph:{type:'website',locale,title:copy['about.label'],description:copy['about.intro']},alternates:{canonical:`/${locale}/${locale==='tr'?'hakkimizda':'about'}`,languages:Object.fromEntries(locales.map(l=>[l,`/${l}/${l==='tr'?'hakkimizda':'about'}`]))}}}
export default function AboutRoute(){return <AboutPage/>}
