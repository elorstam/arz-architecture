import {NextResponse} from 'next/server';import {isAdmin} from '@/lib/admin-auth';import {generateBlog} from '@/lib/ai-cms';import {OpenAIRequestError} from '@/lib/ai-project';
export async function POST(r:Request){if(!await isAdmin())return NextResponse.json({error:'Yetkisiz'},{status:401});try{return NextResponse.json(await generateBlog(await r.json()));}catch(e){const x=e instanceof OpenAIRequestError?e:new OpenAIRequestError('Blog taslağı oluşturulamadı.',500,String(e));console.error('Blog AI failed',{status:x.status,message:x.message,detail:x.detail});return NextResponse.json({error:x.message},{status:x.status===504?504:502});}}
