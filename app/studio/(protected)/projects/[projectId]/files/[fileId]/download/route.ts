import {NextResponse} from "next/server";
import {createStudioProjectFileDownload} from "@/lib/studio/files/file-repository";
export async function GET(_request:Request,{params}:{params:Promise<{projectId:string;fileId:string}>}){try{const{projectId,fileId}=await params;const result=await createStudioProjectFileDownload(projectId,fileId);if(result instanceof Response)return new Response(result.body,{status:200,headers:{"Content-Type":result.headers.get("content-type")??"application/octet-stream","Cache-Control":"private, no-store"}});return NextResponse.redirect(result,{status:303});}catch{return new NextResponse("Dosya bulunamadı veya erişim reddedildi.",{status:404,headers:{"Cache-Control":"private, no-store"}});}}
