import{notFound}from"next/navigation";import StudioProjectStages from"@/components/studio/notifications/StudioProjectStages";import StudioProjectTabs from"@/components/studio/projects/StudioProjectTabs";import{initializeProjectStages,listProjectNotifications,listProjectStages}from"@/lib/studio/notifications/notification-repository";import{listAttachableStageFiles,listStageFiles}from"@/lib/studio/notifications/stage-flex-repository";import{isWhatsAppConfigured}from"@/lib/studio/notifications/whatsapp-adapter";import{getStudioProjectAccess,getStudioProjectById}from"@/lib/studio/projects/project-repository";export const dynamic="force-dynamic";
export default async function Page({params}:{params:Promise<{projectId:string}>}){const{projectId}=await params;const[project,access]=await Promise.all([getStudioProjectById(projectId),getStudioProjectAccess()]);if(!project)notFound();let payload:null|Awaited<ReturnType<typeof load>>=null;try{if(access.canManage)await initializeProjectStages(projectId);payload=await load(projectId)}catch{payload=null}return <main className="mx-auto max-w-[1540px] px-4 py-6 sm:px-6"><p className="text-sm font-semibold text-[#9a7b40]">{project.code}</p><h1 className="mt-1 text-3xl font-semibold">Proje Aşamaları</h1><StudioProjectTabs projectId={projectId} active="stages"/>{payload?<StudioProjectStages projectId={projectId} {...payload} canManage={access.canManage} configured={isWhatsAppConfigured()}/>:<p role="status" className="mt-6 rounded-xl border border-dashed p-6">Migration 017 uygulandıktan sonra esnek aşamalar burada görünecek.</p>}</main>}
async function load(projectId:string){const[stages,notifications,attachments,files]=await Promise.all([listProjectStages(projectId),listProjectNotifications(projectId),listStageFiles(projectId),listAttachableStageFiles(projectId)]);return{stages,notifications,attachments,files}}
