import{redirect}from"next/navigation";import StudioLogoutButton from"@/components/studio/StudioLogoutButton";import{getStudioContext}from"@/lib/studio/auth/get-studio-context";import{createStudioServerClient}from"@/lib/studio/supabase/server";
export const dynamic="force-dynamic";
export default async function StudioProtectedLayout({children}:{children:React.ReactNode}){const context=await getStudioContext().catch(error=>{console.error("Studio context could not be loaded.",error);return null});if(!context?.user)redirect("/studio/login");if(!context.membership){const supabase=await createStudioServerClient();await supabase.rpc("studio_record_activity",{target_organization_id:null,event_entity_type:"auth",event_entity_id:null,event_action:"auth.access_denied",event_summary:"Protected Studio route denied: no active membership.",event_metadata:{}});await supabase.auth.signOut();redirect("/studio/login?reason=access-denied")}return <main className="theme-dark-surface min-h-screen bg-[#0b0b0b] text-white"><header className="border-b border-white/10 px-6 py-4"><div className="mx-auto flex max-w-7xl items-center justify-between"><div><p className="text-[9px] uppercase tracking-[.3em] text-white/35">ARZ Architecture</p><p className="mt-1 text-lg">Studio Core</p></div><StudioLogoutButton/></div></header>{children}</main>}
