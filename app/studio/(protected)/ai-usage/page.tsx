import StudioAiUsageDashboard from"@/components/studio/ai/StudioAiUsageDashboard";import{getStudioAiUsageDashboard,parseStudioAiUsageFilters}from"@/lib/studio/ai/ai-usage-repository";
export default async function StudioAiUsagePage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const raw=await searchParams,simple=Object.fromEntries(Object.entries(raw).map(([key,value])=>[key,Array.isArray(value)?value[0]:value]));const parsed=parseStudioAiUsageFilters(simple);if(!parsed.from&&!parsed.to){const now=new Date();parsed.from=`${now.getUTCFullYear()}-${String(now.getUTCMonth()+1).padStart(2,"0")}-01`;}let data;try{data=await getStudioAiUsageDashboard(parsed)}catch{data={available:false,canViewOrganization:false,events:[],summary:{requests:0,totalTokens:0,estimatedCostUsd:null,successful:0,fallback:0,failed:0,usageUnavailable:0,pricingUnknown:0},modules:[],users:[]}}return <main className="mx-auto w-full min-w-0 max-w-[1540px] px-4 py-6 sm:px-6 sm:py-8 lg:px-8"><header className="mb-7 border-b border-[#ddd8ce] pb-6"><p className="studio-label text-[#9a8253]">Yönetim</p><h1 className="studio-page-title mt-2">AI Kullanımı</h1><p className="studio-body-text mt-3 max-w-3xl text-[#747875]">Organizasyonunuzdaki yapay zekâ kullanımını, token tüketimini ve tahmini maliyeti takip edin.</p></header><StudioAiUsageDashboard data={data} filters={parsed}/></main>}
